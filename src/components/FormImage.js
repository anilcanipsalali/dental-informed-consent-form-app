import React from "react";

const FormImage = ({ image }) => {
  return (
    <img
      className="pb-5 items-center h-36 block m-auto"
      src={image}
      alt="Form Head"
    />
  );
};

export default FormImage;
